//To use whatever font that we want without problems for performance
import { Montserrat } from 'next/font/google';

// Now, we export the font
export const montserrat = Montserrat({ subsets: ['latin'] }) ;